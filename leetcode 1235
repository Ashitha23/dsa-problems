class Solution:
    def jobScheduling(self, startTime: List[int], endTime: List[int], profit: List[int]) -> int:
        jobs=sorted(zip(startTime,endTime,profit))
        n=len(jobs)
        starts=[job[0] for job in jobs]
        s=[0]*(n+1)
        for i in range(n-1,-1,-1):
            x=bisect_left(starts,jobs[i][1])
            y=jobs[i][2]+s[x]
            z=s[i+1]
            s[i]=max(y,z)
        return s[0]
